---
id: kno-01m2ehj9rzfe
title: '[P2] Decide what of the tracked .tickets backlog and the AI design docs ships publicly'
status: open
type: task
priority: 2
mode: hitl
created: '2026-09-13T23:27:46.463043Z'
updated: '2026-09-13T23:27:46.594253Z'
assignee: ''
parent: kno-01m2ebf5sxdb
---

## Description
Pre-public audit finding, severity P2, from the review workflow over main at fcc6b66 on 2026-09-13. Raised by the docs, hygiene lenses and confirmed by an adversarial verifier.

### From the docs lens: Design docs reference private hooks and workflow machinery an outside reader cannot see, and README omits that /digest exists

Evidence: docs/ai-assistant-ideation/*: no secrets, home paths, session ids or hostnames found (grep for https?://, session_, claude.ai, /Users returned only `https://example.test/1` in the plan). But they assume the private devenv-layers environment: kno-01m2ebf3c8mx-test-coverage-plan.md:26-28 'Run every command ... inside devenv shell ... the commit-msg hook rejects Co-Authored-By'; kno-01m2ebf3w4q3-lint-plan.md:17 same; test-coverage-spec.md:138 'the knot-check pre-commit hook'; brainstorms describe 'teams mode, quality-engineer challenger' review cycles. README's 'What it shows' matches app.py ROUTES for all five bullets, but `/digest` (app.py:158) is unmentioned and README:4-6 claim it is 'shaped by the project's own configuration' is borne out by Selection.asked/Project values.

Recommendation: Keep the docs (they explain the design well) but add a one-paragraph docs/ai-assistant-ideation/README.md saying these are AI-assisted working documents tied to knot tickets, and that the hooks they mention come from the maintainer's devenv layer, which CONTRIBUTING should summarise. Optionally mention /digest under 'Live' in README.

Verifier (P2, blocks public: no): Reproduced. docs/ai-assistant-ideation/kno-01m2ebf3c8mx-test-coverage-plan.md:26-28 says to run inside `devenv shell` and that "the commit-msg hook rejects `Co-Authored-By`"; kno-01m2ebf3w4q3-lint-plan.md:17-18 says "If a hook rewrites a file, re-stage"; test-coverage-spec.md:136-138 references "the `knot-check` pre-commit hook" and "the tickets layer selected in `devenv.config.toml`"; both brainstorms (line 4) describe "quality-engineer challenger" / "teams" review modes. None of those hooks are defined in this repo: devenv.nix imports them from `inputs.devenv-layers`, which devenv.yaml pins to `git+ssh://git@github.com/krimsonkla/devenv-layers`, so an outside reader cannot see or run them. Grep for `https?://|session_|claude\.ai|/Users` across docs/ returns only `https://example.test/1` in the plan, so no leakage. README.md's "What it shows" covers the five HTML routes and its "Live" bullet (line 43) already mentions the stream carrying "a digest", but the `/digest` endpoint at src/knotview/panel/app.py:158 is not named; no CONTRIBUTING file exists. This is accurate but a polish item: the docs are design notes that don't mislead about the product, nothing secret is exposed, and a first user of the README-described CLI is unaffected, so P2/non-blocking is correct. (The private ssh-only devenv-layers input itself would be a separate, more serious environment finding, outside this docs item.)

### From the docs lens: Tracked .tickets/ backlog is fine to publish but carries the author's real name as assignee and an in-progress audit note

Evidence: `git ls-files` tracks four open tickets and two archived ones under .tickets/. .tickets/kno-01m2ebf5sxdb--audit-what-must-be-completed-before-the.md:11 and both archive tickets line 11: `assignee: Jason Risch`; the audit ticket body states 'The repository is private and is to be made public' and lists known gaps. Content is otherwise project-relevant and README:3 says the panel is developed against this backlog (also .knot.edn:1-3). The working-tree copy of the audit ticket is currently modified (git status: M) and will need committing or reverting before the publish commit.

Recommendation: Acceptable to publish as-is if the author is comfortable with the name being visible (it already is in git log). Before publishing, close or rewrite the audit ticket so the public backlog does not open with 'is to be made public', and commit the pending modification.

Verifier (P2, blocks public: no): Reproduced: `git ls-files .tickets/` tracks four open tickets and two archived ones; both archive tickets carry `assignee: Jason Risch` at line 11 at HEAD, and the audit ticket body opens with 'The repository is private and is to be made public' and lists the known gaps. Two small corrections to the evidence: in the audit ticket the assignee is at line 10, not 11, and at HEAD (fcc6b66) that ticket still reads `assignee: ''` / `status: open` -- the name, the in_progress status and the 'Starting work...' note exist only in the uncommitted working-tree diff (git status M), so the tracked copy currently carries the name in the archive tickets only. README:3 describes the panel as a viewer over a knot backlog but does not itself say it is developed against this one; that statement is in .knot.edn:1-3. None of this changes the verdict: the name is already the sole author on every commit (`git log` shows one author, Jason Risch <jasonrisch@gmail.com>), the ticket content is project-relevant, and nothing here would break or mislead a first user, so it is polish (P2) and does not block going public. The pending modification should be committed or reverted before the publish commit, and the audit ticket's opening sentence could be rewritten so the public backlog does not lead with 'is to be made public'.

### From the hygiene lens: The project's own backlog and 2,580 lines of AI-workflow design docs ship in the repo and expose process details

Evidence: .knot.edn:1-3 deliberately declares .tickets as the dev fixture, so shipping the tickets is a choice, but what they carry is not curated: .tickets/archive/kno-01m2ebf3w4q3...md:15 "which is why it was committed with --no-verify"; the two archived tickets and the open audit ticket record `assignee: Jason Risch` and branch names `jr/...`; the open audit ticket (line 19) states "there is no LICENSE, no CONTRIBUTING and no CI"; docs/ai-assistant-ideation/* (6 files, 2,580 lines) are brainstorm/spec/plan transcripts referencing "quality-engineer challenger", finding numbers and review cycles. tests/reading/test_real_knot.py:4-5 also says "this repository's ticket-discipline hook blocks bare knot writes", a hook that lives in the private devenv-layers input. The working tree additionally has an uncommitted transition of the audit ticket (`git diff`).

Recommendation: Decide explicitly: keep .tickets as the public sample backlog (then scrub the assignee to a neutral value, fix or drop the --no-verify note once the lint ticket is archived, and mention in README that .tickets is the demo data), and either move docs/ai-assistant-ideation out of the repo or add a one-line README note on what it is. Reword test_real_knot.py:3-5 so it does not depend on a hook outsiders do not have.

Verifier (P2, blocks public: no): Every piece of evidence reproduces at fcc6b66: .knot.edn lines 1-3 explicitly declare .tickets as the project's own backlog and dev fixture; .tickets/archive/kno-01m2ebf3w4q3...md:15 says "committed with --no-verify"; the two archived tickets and the open audit ticket carry "assignee: Jason Risch" and "branch: jr/..." lines (the three other open tickets have assignee ''); the audit ticket line 19 states "no LICENSE, no CONTRIBUTING and no CI" and ls confirms none of those exist; docs/ai-assistant-ideation totals 2,580 lines with two files mentioning quality-engineer/review cycles; tests/reading/test_real_knot.py lines 3-5 reference "this repository's ticket-discipline hook"; git status shows the audit ticket modified but uncommitted. Nothing here is a secret, a credential, or misleading about the code, and the author's name is already in the git log, so no outsider is harmed or blocked by it; the --no-verify note is stale history in an archived ticket, and the test docstring is merely confusing to a contributor without the private hook. This is cosmetic curation of intentionally-shipped sample data and design transcripts, correctly P2 and not a block on going public.
